{
  "name": "fauzan-portfolio-api",
  "version": "1.0.0",
  "description": "Cloudflare Worker API — no external dependencies, free forever",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "kv:create": "wrangler kv namespace create RATE_LIMIT"
  },
  "devDependencies": {
    "wrangler": "^4.94.0"
  }
}
